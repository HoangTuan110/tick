{
  "devDependencies": {
    "mithril": "^2.2.2",
    "esbuild": "^0.17.5",
    "serve": "^14.2.0",
    "@vercel/analytics": "^0.1.8"
  },
  "name": "tick!",
  "version": "1.0.0",
  "description": "How fast can you tick checkboxes?",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "build": "esbuild --bundle index.js --outdir=dist --minify --sourcemap",
    "build-watch": "nodemon --exec yarn build -w index.js",
    "serve-watch": "nodemon --exec serve",
    "serve": "serve"
  }
}
